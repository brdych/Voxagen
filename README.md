A Readme ...



Running the program:

  git clone https://github.com/brdych/Voxagen &&
  cd Voxagen &&
  git submodule update --init --recursive &&
  mkdir build &&
  cd build &&
  cmake .. &&
  make &&
  cd ../bin &&
  ./Voxagen
